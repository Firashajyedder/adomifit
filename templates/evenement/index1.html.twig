{% extends 'base-front.html.twig' %}

{% block title %}A domifit | Evenement{% endblock %}

{% block body %}
  <!-- Breadcrumb Section Begin -->
    <section class="breadcrumb-section set-bg" data-setbg="/front-office/img/breadcrumb-bg.jpg">
        <div class="container">
            <div class="row">
                <div class="col-lg-8 p-0 m-auto">
                    <div class="bh-text">
                        <h3>Les Evenements</h3>
                        <ul>
                            <li>by Admin</li>
                            <li>Aug,15, 2019</li>
                            <li>20 Comment</li>
                        </ul>
                    </div>
                </div>
            </div>
        </div>
    </section>
    <!-- Breadcrumb Section End -->

    

    <!-- Blog Section Begin -->
    <section class="blog-section spad">
        <div class="container">
       
           



            
            </div>      
                </div>
            </div>
        </div>
    </section>
    <!-- Blog Section End -->

  


    <!-- Testimonial Section Begin -->
    <section class="testimonial-section spad">
        <div class="container">
            <div class="row">
                <div class="col-lg-12">
                    <div class="section-title">
                        <span>Testimonial</span>
                        <h2>Our cilent say</h2>
                    </div>
                </div>
            </div>
            <div class="ts_slider owl-carousel">
                <div class="ts_item">
                    <div class="row">
                    {% for e in evenements%}
                        <div class="col-lg-12 text-center">
                            <div class="ti_pic">
                                <img src="{{asset('/uploads/images/' ~  e.image )}}" alt="">
                            </div>
                            <div class="ti_text">
                            <h5><a>{{e.titre}}</a></h5>
                                <p>{{e.description}}</p>
                                <h5><a>{{e.horraire| date("m/d/Y")}}</a></h5>
                            </div>
                            <a href="/listEvenementE/{{e.id}}" class="primary-btn btn-normal appoinment-btn">Avis</a>
                        </div>
                        
                         {% endfor %}
                         
                    </div>
                </div>
                
            </div>
        </div>
    </section>
    
    <!-- Testimonial Section End -->
{% endblock %}
