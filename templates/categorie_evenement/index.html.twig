{% extends 'base-front.html.twig' %}

{% block title %}Hello CategorieEvenementController!{% endblock %}

{% block body %}
<!-- Breadcrumb Section Begin -->
    <section class="breadcrumb-section set-bg" data-setbg="front-office/img/breadcrumb-bg.jpg">
        <div class="container">
            <div class="row">
                <div class="col-lg-8 p-0 m-auto">
                    <div class="bh-text">
                        <h3>Catégorie des Evenements</h3>
                        <ul>
                            <li>by Admin</li>
                            <li>Aug,15, 2019</li>
                            <li>20 Comment</li>
                        </ul>
                    </div>
                </div>
            </div>
        </div>
    </section>
    <!-- Breadcrumb Section End -->
<!-- Team Section Begin -->
    <section class="team-section spad">
    <div class="team-title">
        <div class="container">
            <div class="row">
                <div class="col-lg-12">            
            <div class="row">
                <div class="ts-slider owl-carousel">
                {% for c in categories%}
                    <div class="col-lg-4">
                        <div class="ts-item set-bg" data-setbg="{{asset('uploads/images/' ~  c.image )}}">
                            <div class="ts_text">
                                <h4>{{c.libelle}}</h4>
                                <span>{{c.description}}</span>
                                <a href="{{ path( 'listEvenementC' , {'id':c.id} )}}" class="primary-btn btn-normal appoinment-btn">appointment</a>
                            </div>
                        </div>
                    </div>
                    {% endfor %}
                    
                   
                </div>
            </div>
        </div>
    </section>
    <!-- Team Section End -->
{% endblock %}
